# frozen_string_literal: true

require 'bundler/setup'

require_relative "config/application"
Rails.application.initialize!

desc "The default environment"
task :environment

Dir.glob("tasks/**/*.rake").each { |f| load f }
